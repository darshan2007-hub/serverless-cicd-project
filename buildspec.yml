version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20

  build:
    commands:
      - echo "Build started"
      - zip -r function.zip index.mjs package.json

artifacts:
  files:
    - function.zip